CI failure observed on branch agent/persiste-alias-dedup.

Run: https://github.com/leonardosovienski/automatizacao-curriculo/actions/runs/31248002949

Steps to reproduce / investigate:

1. Open the run link above and expand the failing job and failing step.
2. Copy the error message and stack trace (or screenshot) and paste into this issue (in the **Logs** section).
3. Identify whether the failure is in: dependency install, build, lint, unit tests, or E2E tests.
4. If a unit test failed, run locally: `python -m pytest -q <test_path>::<test_name>` and paste failing output.

Logs:

(paste failing logs here)

Preliminary actions I will take once logs are provided:
- Triage whether the failure is flaky, environment-related (runner/caches), or code regression.
- For flaky/environment issues: propose CI tweak or caching fix in a PR.
- For code regression: propose a fix PR when trivial, otherwise open follow-up issue with reproduction steps.

Priority: high — block merging of this feature branch until resolved.
